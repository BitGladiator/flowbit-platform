# .env.docker - Docker Environment Configuration
# Copy this to .env and update with your values

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRY=20d

# Webhook Configuration (Optional - for n8n integration)
# N8N_WEBHOOK_URL=
# WEBHOOK_SECRET=

# Database Configuration (already set in docker-compose.yml)
# MONGO_URI=mongodb://mongodb:27017/flowbit

# API Configuration (already set in docker-compose.yml)
# NODE_ENV=production
# PORT=3001

# Frontend Configuration (already set in docker-compose.yml)
# REACT_APP_API_URL=http://localhost:3001/api